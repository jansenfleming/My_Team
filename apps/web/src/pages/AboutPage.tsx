// Placeholder about/brand route. Real brand story copy is board task E5, from the
// Creative Director's D4 spec (docs/design/about.md, not written yet). Any fact about the
// owner or the brand's founding must stay [PLACEHOLDER: ...] until supplied — this page
// intentionally has none of its own yet.
export function AboutPage() {
  return (
    <section className="page page-about">
      <h1>About</h1>
      <p className="placeholder-copy">
        Brand story lands once D4 (docs/design/about.md) is written and built (board task
        E5). [PLACEHOLDER: brand story copy.]
      </p>
    </section>
  );
}
