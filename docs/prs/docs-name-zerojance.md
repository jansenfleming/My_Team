# docs/name-zerojance: site name is ZEROJANCE

Author: architect. Branch: `docs/name-zerojance`. Base: `main` (b7227a9). Docs only.

## What changed
- `docs/architecture/board.md`: site name recorded as ZEROJANCE (owner's decision via the lead; supersedes PICKET-07, GRAYDOT, PARZIVAL); notes that PR #1 (`docs/real-pr-flow`, b7227a9) is merged and done; the Creative Director rename note now names ZEROJANCE and says earlier rename drafts must not be merged.
- Nothing else in `docs/architecture/` names the site. Historical review and PR files (`reviews/feat-design-concept.md`, `prs/feat-design-concept.md`, `prs/docs-real-pr-flow.md`) keep the old working names as history.

## How to review
Read the diff. No code, no tests apply.
